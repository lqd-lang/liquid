extern {
    fn print_hello_world -> void;
    fn print_num(x: int) -> void;
    fn get_num -> int;
}

fn main -> uint {
    print_hello_world();

    is_bad(get_num());
    
    0
}

fn is_bad(x: int) -> bool {
    x >= 15
}

fn not(x: bool) -> bool {
    x == false
}